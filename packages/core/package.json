{
  "name": "core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "find src -name '*.test.ts' | xargs node --test --experimental-strip-types"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}

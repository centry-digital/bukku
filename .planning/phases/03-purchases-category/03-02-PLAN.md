---
phase: 03-purchases-category
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/configs/purchase-order.ts
  - src/tools/configs/goods-received-note.ts
  - src/tools/configs/purchase-bill.ts
  - src/tools/configs/purchase-credit-note.ts
  - src/tools/configs/purchase-payment.ts
  - src/tools/configs/purchase-refund.ts
autonomous: true

must_haves:
  truths:
    - "6 purchase entity config files exist with correct CrudEntityConfig structure"
    - "Each config maps to the correct Bukku API path under /purchases/"
    - "Each config has entity-specific listFilters matching the OpenAPI spec exactly"
    - "All configs include corrected businessRules (draft+void delete, pending_approval lifecycle)"
    - "Purchase bill config includes payment_mode filter and documents the 'claim' option"
    - "Entity names follow locked naming: purchase-order, goods-received-note, purchase-bill, purchase-credit-note, purchase-payment, purchase-refund"
  artifacts:
    - path: "src/tools/configs/purchase-order.ts"
      provides: "Purchase order entity config"
      exports: ["purchaseOrderConfig"]
    - path: "src/tools/configs/goods-received-note.ts"
      provides: "Goods received note entity config"
      exports: ["goodsReceivedNoteConfig"]
    - path: "src/tools/configs/purchase-bill.ts"
      provides: "Purchase bill entity config with payment_mode filter"
      exports: ["purchaseBillConfig"]
    - path: "src/tools/configs/purchase-credit-note.ts"
      provides: "Purchase credit note entity config"
      exports: ["purchaseCreditNoteConfig"]
    - path: "src/tools/configs/purchase-payment.ts"
      provides: "Purchase payment entity config"
      exports: ["purchasePaymentConfig"]
    - path: "src/tools/configs/purchase-refund.ts"
      provides: "Purchase refund entity config"
      exports: ["purchaseRefundConfig"]
  key_links:
    - from: "src/tools/configs/purchase-*.ts"
      to: "src/types/bukku.ts"
      via: "import type { CrudEntityConfig }"
      pattern: "import type.*CrudEntityConfig.*bukku"
    - from: "src/tools/configs/purchase-bill.ts"
      to: "factory listFilters"
      via: "payment_mode in listFilters array"
      pattern: "payment_mode"
---

<objective>
Create CrudEntityConfig objects for all 6 purchase entities, following the validated sales config pattern from Phase 2.

Purpose: These configs are the sole input needed for the factory to generate 36 purchase MCP tools. Each config maps a purchase entity to its Bukku API endpoints, filters, and business rules.

Output: 6 new config files in src/tools/configs/
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-purchases-category/03-CONTEXT.md
@.planning/phases/03-purchases-category/03-RESEARCH.md
@src/types/bukku.ts
@src/tools/configs/sales-invoice.ts
@src/tools/configs/sales-payment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create purchase order and goods received note configs</name>
  <files>
    src/tools/configs/purchase-order.ts
    src/tools/configs/goods-received-note.ts
  </files>
  <action>
    Create two new CrudEntityConfig files following the exact pattern from sales configs (same import, same JSDoc style, same field structure).

    **src/tools/configs/purchase-order.ts:**
    ```
    entity: "purchase-order"
    apiBasePath: "/purchases/orders"
    singularKey: "transaction"
    pluralKey: "transactions"
    description: "purchase order"
    operations: ["list", "get", "create", "update", "delete"]
    hasStatusUpdate: true
    listFilters: ["contact_id", "email_status", "transfer_status"]
    businessRules:
      delete: "Only draft and void purchase orders can be deleted. Ready or pending approval purchase orders cannot be deleted — use update-purchase-order-status to void a ready purchase order instead."
      statusTransitions: "Valid transitions: draft -> pending_approval, draft -> ready, pending_approval -> ready, ready -> void. A void purchase order is final and cannot be changed. There is no way to revert from ready, pending_approval, or void back to draft."
    ```
    Export name: `purchaseOrderConfig`

    **src/tools/configs/goods-received-note.ts:**
    ```
    entity: "goods-received-note"
    apiBasePath: "/purchases/goods_received_notes"
    singularKey: "transaction"
    pluralKey: "transactions"
    description: "goods received note"
    operations: ["list", "get", "create", "update", "delete"]
    hasStatusUpdate: true
    listFilters: ["contact_id", "email_status", "transfer_status"]
    businessRules:
      delete: "Only draft and void goods received notes can be deleted. Ready or pending approval goods received notes cannot be deleted — use update-goods-received-note-status to void a ready goods received note instead."
      statusTransitions: "Valid transitions: draft -> pending_approval, draft -> ready, pending_approval -> ready, ready -> void. A void goods received note is final and cannot be changed. There is no way to revert from ready, pending_approval, or void back to draft."
    ```
    Export name: `goodsReceivedNoteConfig`

    Use the full name "goods-received-note" per user decision (no GRN abbreviation).
    JSDoc comment format: `API: /purchases/{path}`, `Response keys: transaction (singular), transactions (plural)`.
  </action>
  <verify>
    1. `npx tsc --noEmit` passes (configs type-check against CrudEntityConfig)
    2. Both files export their config object correctly
    3. `grep "goods-received-note" src/tools/configs/goods-received-note.ts` confirms full name used
  </verify>
  <done>Purchase order and goods received note configs created with correct API paths, filters per OpenAPI, and corrected business rules.</done>
</task>

<task type="auto">
  <name>Task 2: Create bill, credit note, payment, and refund configs</name>
  <files>
    src/tools/configs/purchase-bill.ts
    src/tools/configs/purchase-credit-note.ts
    src/tools/configs/purchase-payment.ts
    src/tools/configs/purchase-refund.ts
  </files>
  <action>
    Create four CrudEntityConfig files. Pay close attention to entity-specific filter differences.

    **src/tools/configs/purchase-bill.ts:**
    ```
    entity: "purchase-bill"
    apiBasePath: "/purchases/bills"
    singularKey: "transaction"
    pluralKey: "transactions"
    description: "purchase bill"
    operations: ["list", "get", "create", "update", "delete"]
    hasStatusUpdate: true
    listFilters: ["contact_id", "payment_status", "payment_mode"]
    ```
    IMPORTANT: Bills do NOT have email_status filter (verified in OpenAPI purchase.yaml). Bills DO have payment_mode with credit/cash/claim enum.
    Add JSDoc note: `Note: payment_mode filter includes 'claim' option for expense claims/reimbursements`
    businessRules: same corrected template with "bills" / "bill" / "update-purchase-bill-status"
    Export name: `purchaseBillConfig`

    **src/tools/configs/purchase-credit-note.ts:**
    ```
    entity: "purchase-credit-note"
    apiBasePath: "/purchases/credit_notes"
    singularKey: "transaction"
    pluralKey: "transactions"
    description: "purchase credit note"
    operations: ["list", "get", "create", "update", "delete"]
    hasStatusUpdate: true
    listFilters: ["contact_id", "payment_status"]
    ```
    IMPORTANT: Credit notes do NOT have email_status filter (verified in OpenAPI purchase.yaml).
    businessRules: same corrected template with "credit notes" / "credit note" / "update-purchase-credit-note-status"
    Export name: `purchaseCreditNoteConfig`

    **src/tools/configs/purchase-payment.ts:**
    ```
    entity: "purchase-payment"
    apiBasePath: "/purchases/payments"
    singularKey: "transaction"
    pluralKey: "transactions"
    description: "purchase payment"
    operations: ["list", "get", "create", "update", "delete"]
    hasStatusUpdate: true
    listFilters: ["contact_id", "email_status", "payment_status", "account_id"]
    ```
    businessRules: same corrected template with "payments" / "payment" / "update-purchase-payment-status"
    Export name: `purchasePaymentConfig`

    **src/tools/configs/purchase-refund.ts:**
    ```
    entity: "purchase-refund"
    apiBasePath: "/purchases/refunds"
    singularKey: "transaction"
    pluralKey: "transactions"
    description: "purchase refund"
    operations: ["list", "get", "create", "update", "delete"]
    hasStatusUpdate: true
    listFilters: ["contact_id", "email_status", "payment_status", "account_id"]
    ```
    businessRules: same corrected template with "refunds" / "refund" / "update-purchase-refund-status"
    Export name: `purchaseRefundConfig`

    All businessRules use the corrected template:
    - delete: "Only draft and void {plural} can be deleted. Ready or pending approval {plural} cannot be deleted — use {status-tool} to void a ready {singular} instead."
    - statusTransitions: "Valid transitions: draft -> pending_approval, draft -> ready, pending_approval -> ready, ready -> void. A void {singular} is final and cannot be changed. There is no way to revert from ready, pending_approval, or void back to draft."
  </action>
  <verify>
    1. `npx tsc --noEmit` passes
    2. `grep "email_status" src/tools/configs/purchase-bill.ts` returns nothing (bills lack email_status)
    3. `grep "email_status" src/tools/configs/purchase-credit-note.ts` returns nothing (credit notes lack email_status)
    4. `grep "payment_mode" src/tools/configs/purchase-bill.ts` returns a match (bills have payment_mode)
    5. `grep "account_id" src/tools/configs/purchase-payment.ts src/tools/configs/purchase-refund.ts` returns 2 matches
    6. All 6 purchase config files contain "draft and void" and "pending_approval"
  </verify>
  <done>All 4 remaining purchase entity configs created. Bills and credit notes correctly omit email_status. Bills include payment_mode. Payments and refunds include account_id. All use corrected business rules.</done>
</task>

</tasks>

<verification>
- 6 new config files exist in src/tools/configs/
- Entity names match locked decisions: purchase-order, goods-received-note, purchase-bill, purchase-credit-note, purchase-payment, purchase-refund
- API paths correct: /purchases/orders, /purchases/goods_received_notes, /purchases/bills, /purchases/credit_notes, /purchases/payments, /purchases/refunds
- Filter parity with OpenAPI:
  - Purchase Order: contact_id, email_status, transfer_status
  - Goods Received Note: contact_id, email_status, transfer_status
  - Bill: contact_id, payment_status, payment_mode (NO email_status)
  - Credit Note: contact_id, payment_status (NO email_status)
  - Payment: contact_id, email_status, payment_status, account_id
  - Refund: contact_id, email_status, payment_status, account_id
- All configs have corrected businessRules with "draft and void" delete + pending_approval lifecycle
- TypeScript compiles without errors
</verification>

<success_criteria>
- 6 purchase entity configs created matching CrudEntityConfig interface
- All configs type-check against bukku.ts types
- Filter lists match OpenAPI spec exactly (especially: no email_status on bills/credit notes)
- Business rules consistent with corrected template across all 6 configs
</success_criteria>

<output>
After completion, create `.planning/phases/03-purchases-category/03-02-SUMMARY.md`
</output>

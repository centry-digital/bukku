---
phase: 03-purchases-category
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/configs/sales-quote.ts
  - src/tools/configs/sales-order.ts
  - src/tools/configs/delivery-order.ts
  - src/tools/configs/sales-invoice.ts
  - src/tools/configs/sales-credit-note.ts
  - src/tools/configs/sales-payment.ts
  - src/tools/configs/sales-refund.ts
autonomous: true

must_haves:
  truths:
    - "All 7 sales entity configs state that draft AND void transactions can be deleted"
    - "All 7 sales entity configs include pending_approval in the status lifecycle"
    - "Status transitions describe both direct path (draft -> ready) and approval path (draft -> pending_approval -> ready)"
    - "No other config fields change (entity, apiBasePath, listFilters, etc. remain identical)"
  artifacts:
    - path: "src/tools/configs/sales-quote.ts"
      provides: "Corrected business rules for sales quotes"
      contains: "draft and void"
    - path: "src/tools/configs/sales-order.ts"
      provides: "Corrected business rules for sales orders"
      contains: "draft and void"
    - path: "src/tools/configs/delivery-order.ts"
      provides: "Corrected business rules for delivery orders"
      contains: "draft and void"
    - path: "src/tools/configs/sales-invoice.ts"
      provides: "Corrected business rules for sales invoices"
      contains: "draft and void"
    - path: "src/tools/configs/sales-credit-note.ts"
      provides: "Corrected business rules for sales credit notes"
      contains: "draft and void"
    - path: "src/tools/configs/sales-payment.ts"
      provides: "Corrected business rules for sales payments"
      contains: "draft and void"
    - path: "src/tools/configs/sales-refund.ts"
      provides: "Corrected business rules for sales refunds"
      contains: "draft and void"
  key_links:
    - from: "src/tools/configs/*.ts (all 7 sales)"
      to: "src/tools/factory.ts"
      via: "businessRules.delete and businessRules.statusTransitions read by factory"
      pattern: "businessRules.*delete.*draft and void"
---

<objective>
Correct business rules in all 7 sales entity configs to fix two inaccuracies discovered during Phase 3 discussion.

Purpose: Current sales configs state incorrect delete constraint ("only draft") and omit the pending_approval status from lifecycle. Per user decision, both sales AND purchases share the same corrected rules. Fixing sales first ensures consistency when purchase configs are created in parallel.

Output: 7 updated sales config files with accurate businessRules
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-purchases-category/03-CONTEXT.md
@src/tools/configs/sales-quote.ts
@src/tools/configs/sales-order.ts
@src/tools/configs/delivery-order.ts
@src/tools/configs/sales-invoice.ts
@src/tools/configs/sales-credit-note.ts
@src/tools/configs/sales-payment.ts
@src/tools/configs/sales-refund.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct business rules in all 7 sales entity configs</name>
  <files>
    src/tools/configs/sales-quote.ts
    src/tools/configs/sales-order.ts
    src/tools/configs/delivery-order.ts
    src/tools/configs/sales-invoice.ts
    src/tools/configs/sales-credit-note.ts
    src/tools/configs/sales-payment.ts
    src/tools/configs/sales-refund.ts
  </files>
  <action>
    Update the `businessRules` object in each of the 7 sales entity config files. Only the `businessRules.delete` and `businessRules.statusTransitions` strings change. All other fields (entity, apiBasePath, singularKey, pluralKey, description, operations, hasStatusUpdate, listFilters) MUST remain exactly as they are.

    Per user decision in CONTEXT.md, two corrections apply:

    **Correction 1 — Delete constraint:**
    Change from: "Only draft {entity-type} can be deleted."
    Change to: "Only draft and void {entity-type} can be deleted. Ready or pending approval {entity-type} cannot be deleted — use update-{entity}-status to void a ready {entity-type} instead."

    **Correction 2 — Status transitions:**
    Change from: "Valid transitions: draft -> ready, ready -> void."
    Change to: "Valid transitions: draft -> pending_approval, draft -> ready, pending_approval -> ready, ready -> void. A void {entity-type} is final and cannot be changed. There is no way to revert from ready, pending_approval, or void back to draft."

    Apply these corrections to each file, replacing {entity-type} with the entity's description noun:
    - sales-quote.ts: "quotes" / "quote" (entity: sales-quote, status tool: update-sales-quote-status)
    - sales-order.ts: "orders" / "order" (entity: sales-order, status tool: update-sales-order-status)
    - delivery-order.ts: "delivery orders" / "delivery order" (entity: delivery-order, status tool: update-delivery-order-status)
    - sales-invoice.ts: "invoices" / "invoice" (entity: sales-invoice, status tool: update-sales-invoice-status)
    - sales-credit-note.ts: "credit notes" / "credit note" (entity: sales-credit-note, status tool: update-sales-credit-note-status)
    - sales-payment.ts: "payments" / "payment" (entity: sales-payment, status tool: update-sales-payment-status)
    - sales-refund.ts: "refunds" / "refund" (entity: sales-refund, status tool: update-sales-refund-status)

    Do NOT change any other fields in the configs. The only changes should be the two businessRules string values.
  </action>
  <verify>
    Run these checks:
    1. `grep -r "Only draft and void" src/tools/configs/sales-*.ts src/tools/configs/delivery-order.ts | wc -l` should return 7
    2. `grep -r "pending_approval" src/tools/configs/sales-*.ts src/tools/configs/delivery-order.ts | wc -l` should return 7
    3. `grep -r "Only draft quotes can\|Only draft orders can\|Only draft invoices can\|Only draft credit notes can\|Only draft payments can\|Only draft refunds can\|Only draft delivery orders can" src/tools/configs/` should return 0 (old text gone)
    4. `npx tsc --noEmit` passes (no type errors)
  </verify>
  <done>All 7 sales entity configs have corrected businessRules: delete constraint says "draft and void" (not "draft only"), and statusTransitions includes pending_approval path. No other config fields changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds with corrected sales configs</name>
  <files>(no new files — build verification only)</files>
  <action>
    Run the full build to confirm corrected sales configs don't break anything:
    1. `npm run build` — TypeScript compilation succeeds
    2. `npm test` — All existing tests pass
    3. Verify the 42 sales tools are still generated by checking build output or running a quick smoke test

    If any test or build fails, the issue is in the businessRules string edits (likely a syntax error like missing quote or comma). Fix and re-run.
  </action>
  <verify>
    1. `npm run build` exits 0
    2. `npm test` exits 0 with all tests passing
  </verify>
  <done>Build and tests pass with corrected sales business rules. No regressions.</done>
</task>

</tasks>

<verification>
- All 7 sales config files contain "draft and void" in delete rule
- All 7 sales config files contain "pending_approval" in status transitions
- Zero occurrences of old "Only draft {type} can be deleted" pattern
- TypeScript compiles without errors
- All existing tests pass
</verification>

<success_criteria>
- 7 sales entity configs updated with corrected businessRules
- Delete constraint: "draft and void" (not "draft only")
- Status lifecycle: includes pending_approval path
- Build passes, tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-purchases-category/03-01-SUMMARY.md`
</output>

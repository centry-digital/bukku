---
phase: 02-sales-category
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/tools/registry.ts
autonomous: true

must_haves:
  truths:
    - "MCP server registers exactly 42 sales tools on startup"
    - "All 7 entities produce 6 tools each (list, get, create, update, delete, update-status)"
    - "Tool names follow kebab-case convention (list-sales-invoices, create-delivery-order, etc.)"
    - "Server builds and starts without errors"
  artifacts:
    - path: "src/tools/registry.ts"
      provides: "Tool registration orchestration for all 7 sales entities"
      contains: "registerCrudTools"
      min_lines: 20
  key_links:
    - from: "src/tools/registry.ts"
      to: "src/tools/configs/*.ts"
      via: "import of entity configs"
      pattern: "import.*Config.*from.*configs/"
    - from: "src/tools/registry.ts"
      to: "src/tools/factory.ts"
      via: "registerCrudTools function call"
      pattern: "registerCrudTools\\(server, client,"
    - from: "src/index.ts"
      to: "src/tools/registry.ts"
      via: "registerAllTools call on startup"
      pattern: "registerAllTools\\(server, client\\)"
---

<objective>
Wire all 7 sales entity configs into the tool registry and verify the complete build produces 42 working MCP tools.

Purpose: This is the final wiring step that connects Phase 1 infrastructure to Phase 2 business logic. After this, the MCP server exposes all sales CRUD operations to Claude.

Output: Updated registry.ts that registers 42 tools (7 entities x 6 operations each). Build passes, server starts successfully.
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sales-category/02-CONTEXT.md
@.planning/phases/02-sales-category/02-01-SUMMARY.md

@src/tools/registry.ts
@src/tools/factory.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire entity configs into registry</name>
  <files>src/tools/registry.ts</files>
  <action>
Update `src/tools/registry.ts` to:

1. Import `registerCrudTools` from `./factory.js`
2. Import all 7 entity configs from their config files:
   - `salesQuoteConfig` from `./configs/sales-quote.js`
   - `salesOrderConfig` from `./configs/sales-order.js`
   - `deliveryOrderConfig` from `./configs/delivery-order.js`
   - `salesInvoiceConfig` from `./configs/sales-invoice.js`
   - `salesCreditNoteConfig` from `./configs/sales-credit-note.js`
   - `salesPaymentConfig` from `./configs/sales-payment.js`
   - `salesRefundConfig` from `./configs/sales-refund.js`

3. In `registerAllTools()`, call `registerCrudTools(server, client, config)` for each of the 7 configs and accumulate the tool count.

4. Add a comment grouping them as "Sales entities (Phase 2)" for future phase additions.

The registry already has the function signature `registerAllTools(server: McpServer, client: BukkuClient): number` -- just replace the empty body (currently `return 0;`) with the registration calls.

Expected return value: 42 (7 entities x 6 tools each: list, get, create, update, delete, update-status).

Use `.js` extensions in import paths (ESM requirement per Phase 1 decision).
  </action>
  <verify>
Run `npx tsc --noEmit` -- build compiles with zero errors. Then run `npm run build` to produce build output.
  </verify>
  <done>
registry.ts imports all 7 configs and registers them via factory. Build compiles cleanly. registerAllTools returns 42.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full build and tool count</name>
  <files>src/tools/registry.ts</files>
  <action>
Verify the complete integration works end-to-end:

1. Run `npm run build` -- must compile with zero errors and produce build/ output
2. Run `npm test` -- existing tests must still pass (error transformer tests from Phase 1)
3. Verify the tool count: Add a simple check by reading the built registry.ts output or adding a temporary test/script that imports registerAllTools and confirms it would register 42 tools.

The expected tool names (42 total):
- list-sales-quotes, get-sales-quote, create-sales-quote, update-sales-quote, delete-sales-quote, update-sales-quote-status
- list-sales-orders, get-sales-order, create-sales-order, update-sales-order, delete-sales-order, update-sales-order-status
- list-delivery-orders, get-delivery-order, create-delivery-order, update-delivery-order, delete-delivery-order, update-delivery-order-status
- list-sales-invoices, get-sales-invoice, create-sales-invoice, update-sales-invoice, delete-sales-invoice, update-sales-invoice-status
- list-sales-credit-notes, get-sales-credit-note, create-sales-credit-note, update-sales-credit-note, delete-sales-credit-note, update-sales-credit-note-status
- list-sales-payments, get-sales-payment, create-sales-payment, update-sales-payment, delete-sales-payment, update-sales-payment-status
- list-sales-refunds, get-sales-refund, create-sales-refund, update-sales-refund, delete-sales-refund, update-sales-refund-status

If build or tests fail, fix the issues. Common issues:
- Missing `.js` extension in imports (ESM)
- Incorrect relative path depths (configs are in src/tools/configs/, types are in src/types/)
- TypeScript strict mode catching any type mismatches
  </action>
  <verify>
`npm run build` exits 0. `npm test` passes. Server startup log shows "42 tools registered" (verify via `node build/index.js 2>&1 | head -5` or checking stderr output -- will fail without env vars but log line should appear before that if tool registration happens first, OR just verify the build output exists and the imports are correct).
  </verify>
  <done>
Build produces clean output in build/. Existing tests pass. All 42 tools are registered via the factory pattern. Phase 2 sales tools are ready for use.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. `npm test` passes (existing Phase 1 tests unaffected)
3. registry.ts imports all 7 configs and calls registerCrudTools for each
4. Total tool count = 42 (7 entities x 6 operations)
5. Build output in build/ includes all config files and updated registry
</verification>

<success_criteria>
MCP server build succeeds with 42 sales tools registered. The factory pattern generates correct tool names (kebab-case), descriptions (functional), and schemas (uniform filters + entity-specific filters) for all 7 sales entities. Existing Phase 1 tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sales-category/02-02-SUMMARY.md`
</output>

---
phase: 05-products-lists
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/errors/transform.ts
  - src/errors/transform.test.ts
  - tsconfig.json
  - src/tools/configs/product-bundle.ts
  - src/tools/registry.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "500 errors from Bukku API include the server response body so hidden validation errors are visible"
    - "list-product-bundles tool does not exist (no 404 from non-existent endpoint)"
    - "Users are told to use list-products with type=bundle to list bundles"
    - "Server reports 136 tools (not 137)"
  artifacts:
    - path: "src/errors/transform.ts"
      provides: "Enhanced 500 error handler with response body"
      contains: "JSON.stringify"
    - path: "src/errors/transform.test.ts"
      provides: "Test confirming 500 error includes response body"
      contains: "Server response"
    - path: "src/tools/configs/product-bundle.ts"
      provides: "Bundle config without list operation"
      contains: "get.*create.*update.*delete"
    - path: "src/tools/registry.ts"
      provides: "Updated tool count comment"
      contains: "136"
  key_links:
    - from: "src/errors/transform.ts"
      to: "500 handler"
      via: "body included in error message"
      pattern: "JSON\\.stringify.*parsedBody"
    - from: "src/tools/configs/product-bundle.ts"
      to: "src/tools/registry.ts"
      via: "registerCrudTools generates 4 tools instead of 5"
      pattern: 'operations.*get.*create.*update.*delete'
---

<objective>
Close two UAT gaps from Phase 5 testing:

1. **Gap 1 (update-product 500):** The error transform already enhanced in working tree -- 500 errors now include the response body. Need to add a test for the new behavior and commit the fix.

2. **Gap 2 (list-product-bundles 404):** Remove the non-existent "list" operation from product bundle config. Bukku API has no GET /products/bundles endpoint. Bundles are listed via GET /products?type=bundle (the list-products tool). Update description to guide users. Tool count drops from 137 to 136.

Purpose: Fix two major UAT issues -- one surfacing hidden API errors, one removing a tool for a non-existent endpoint.
Output: Working codebase with 136 tools, enhanced error messages, no phantom bundle list tool.
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-products-lists/05-UAT.md
@src/errors/transform.ts
@src/errors/transform.test.ts
@src/tools/configs/product-bundle.ts
@src/tools/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize 500 error body inclusion and add test</name>
  <files>src/errors/transform.ts, src/errors/transform.test.ts, tsconfig.json</files>
  <action>
The working tree already contains the fix for Gap 1:
- `src/errors/transform.ts` -- 500 handler now includes `JSON.stringify(parsedBody)` in error message
- `src/errors/transform.test.ts` -- import changed from `.js` to `.ts` extension
- `tsconfig.json` -- excludes test files from compilation

These changes are correct and tests pass. Add one new test case to `transform.test.ts` that verifies the new 500 behavior:

```typescript
test('500 error includes response body for debugging', () => {
  const result = transformHttpError(
    500,
    { message: 'Internal error', errors: { name: ['is already taken'] } },
    'update product'
  );

  assert.strictEqual(result.isError, true);
  const text = result.content[0].text;
  assert.ok(text.includes('Server response:'), 'Should include server response label');
  assert.ok(text.includes('is already taken'), 'Should include actual error details from body');
});
```

This test proves the gap is closed: 500 errors now surface hidden validation details that Bukku returns incorrectly as 500s.

Run `node --experimental-strip-types --test src/errors/transform.test.ts` to verify all 11 tests pass.
  </action>
  <verify>
`node --experimental-strip-types --test src/errors/transform.test.ts` passes with 11 tests (10 existing + 1 new).
`npx tsc --noEmit` compiles cleanly.
  </verify>
  <done>
500 error handler includes response body in error messages. New test confirms hidden validation errors inside 500 responses are surfaced. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove phantom list-product-bundles tool</name>
  <files>src/tools/configs/product-bundle.ts, src/tools/registry.ts</files>
  <action>
**In `src/tools/configs/product-bundle.ts`:**

1. Change operations array from `["list", "get", "create", "update", "delete"]` to `["get", "create", "update", "delete"]` (remove "list").

2. Update the description to guide users to use list-products with type=bundle:
   ```
   "product bundle. Bundles aggregate multiple products with optional discounts. To list bundles, use list-products with type=bundle. Use list-products to find product IDs for bundle items."
   ```

3. Update the JSDoc comment to document why list is excluded:
   ```
   * Note: Bukku API has no GET /products/bundles endpoint.
   * To list bundles, use GET /products?type=bundle (the list-products tool with type=bundle filter).
   ```

**In `src/tools/registry.ts`:**

1. Update the Phase 5 comment block for product bundle from "5 tools" to "4 tools":
   ```
   // Product bundle: 4 tools (no list — use list-products with type=bundle; no status — archive handled by custom tools)
   ```

2. Update the Phase 5 total in the top-level JSDoc from "15 factory tools" to "14 factory tools" and "29 tools" to "28 tools":
   ```
   * Phase 5 added product entity configs (3 entities, 14 factory tools + 4 custom archive tools + 10 reference data tools = 28 tools).
   ```

The tool count decreases from 137 to 136.

Run `npx tsc --noEmit` and `npm run build` to verify compilation. Then verify by running `node build/index.js 2>&1 | head -5` (or equivalent) to confirm the server reports 136 tools on startup.
  </action>
  <verify>
`npx tsc --noEmit` compiles cleanly.
`npm run build` succeeds.
Server startup log shows 136 tools registered (not 137).
`grep -c '"list"' src/tools/configs/product-bundle.ts` returns 0 (no list operation).
  </verify>
  <done>
list-product-bundles tool no longer exists. Bundle config generates 4 tools (get, create, update, delete). Description directs users to list-products with type=bundle. Registry comments reflect 136 total tools.
  </done>
</task>

</tasks>

<verification>
1. All existing tests pass: `node --experimental-strip-types --test src/errors/transform.test.ts`
2. New test for 500 body inclusion passes
3. TypeScript compiles: `npx tsc --noEmit`
4. Build succeeds: `npm run build`
5. Server reports 136 tools on startup
6. No "list-product-bundles" tool exists in the registry
7. Bundle description mentions "use list-products with type=bundle"
</verification>

<success_criteria>
- 500 errors from Bukku API include the response body in error messages (tested)
- list-product-bundles tool removed (bundle config has no "list" operation)
- Bundle description guides users to list-products with type=bundle
- Server starts with 136 tools
- All tests pass, TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-products-lists/05-04-SUMMARY.md`
</output>

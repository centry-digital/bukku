---
phase: 05-products-lists
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/tools/registry.ts

autonomous: true

must_haves:
  truths:
    - "MCP server starts and reports correct total tool count"
    - "All product CRUD tools are accessible (list, get, create, update, delete for products, bundles, groups)"
    - "All product archive/unarchive tools are accessible (archive-product, unarchive-product, archive-product-bundle, unarchive-product-bundle)"
    - "All reference data list tools are accessible (list-tax-codes, list-currencies, list-payment-methods, etc.)"
    - "Build compiles with zero errors"
    - "All existing tests pass"
  artifacts:
    - path: "src/tools/registry.ts"
      provides: "Complete tool registry with Phase 5 entities"
      exports: ["registerAllTools"]
  key_links:
    - from: "src/tools/registry.ts"
      to: "src/tools/configs/product.ts"
      via: "import productConfig"
      pattern: "import.*productConfig"
    - from: "src/tools/registry.ts"
      to: "src/tools/configs/product-bundle.ts"
      via: "import productBundleConfig"
      pattern: "import.*productBundleConfig"
    - from: "src/tools/registry.ts"
      to: "src/tools/configs/product-group.ts"
      via: "import productGroupConfig"
      pattern: "import.*productGroupConfig"
    - from: "src/tools/registry.ts"
      to: "src/tools/custom/product-archive.ts"
      via: "import registerProductArchiveTools"
      pattern: "import.*registerProductArchiveTools"
    - from: "src/tools/registry.ts"
      to: "src/tools/custom/reference-data.ts"
      via: "import registerReferenceDataTools"
      pattern: "import.*registerReferenceDataTools"
    - from: "src/tools/registry.ts"
      to: "src/tools/cache/reference-cache.ts"
      via: "instantiate ReferenceDataCache"
      pattern: "new ReferenceDataCache"
---

<objective>
Wire all Phase 5 product entity configs, custom archive tools, and reference data tools into the tool registry. Verify the complete build with all tests passing.

Purpose: This is the integration step that makes all Phase 5 tools available to Claude. The registry is the single point of orchestration for all MCP tools.

Output: Updated registry.ts with Phase 5 entities, verified build producing ~137 tools (108 prior + 15 product factory + 4 product archive + 10 reference data).
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/tools/registry.ts
@.planning/phases/05-products-lists/05-01-SUMMARY.md
@.planning/phases/05-products-lists/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 5 entities into registry</name>
  <files>
    src/tools/registry.ts
  </files>
  <action>
Update src/tools/registry.ts to import and register all Phase 5 entities. Follow the established pattern from Phase 4.

**Add imports at the top of the file, grouped by phase:**

```typescript
// Product entities (Phase 5)
import { productConfig } from "./configs/product.js";
import { productBundleConfig } from "./configs/product-bundle.js";
import { productGroupConfig } from "./configs/product-group.js";

// Custom tools (Phase 5)
import { registerProductArchiveTools } from "./custom/product-archive.js";
import { registerReferenceDataTools } from "./custom/reference-data.js";

// Cache (Phase 5)
import { ReferenceDataCache } from "./cache/reference-cache.js";
```

**Update the JSDoc header** to mention Phase 5:
```
* Phase 5 added product entity configs (3 entities, 15 factory tools + 4 custom archive tools + 10 reference data tools = 29 tools).
```

**Inside registerAllTools function, add Phase 5 section after the Phase 4 custom tools:**

```typescript
// Product entities (Phase 5)
// Product: 5 tools (no status -- archive handled by custom tools)
// Product bundle: 5 tools (no status -- archive handled by custom tools)
// Product group: 5 tools (no status -- groups have no archive)
totalTools += registerCrudTools(server, client, productConfig);
totalTools += registerCrudTools(server, client, productBundleConfig);
totalTools += registerCrudTools(server, client, productGroupConfig);

// Custom product archive tools (Phase 5) -- 4 tools
totalTools += registerProductArchiveTools(server, client);

// Reference data list tools (Phase 5) -- 10 tools
// Uses transparent cache with 5-minute TTL for reference data
const referenceCache = new ReferenceDataCache();
totalTools += registerReferenceDataTools(server, client, referenceCache);
```

Key points:
- Factory registrations first (products, bundles, groups)
- Custom archive tools after factory (established pattern from Phase 4)
- Reference data tools last (new tool category)
- ReferenceDataCache instantiated inside registerAllTools with default 5-minute TTL
- Comment blocks with tool counts for clarity

Expected total: 108 (prior) + 5 (product) + 5 (bundle) + 5 (group) + 4 (archive) + 10 (reference) = 137 tools.
  </action>
  <verify>
Run the following in sequence:
1. `npx tsc --noEmit` -- TypeScript compilation passes
2. `npm run build` -- Full build succeeds
3. `npm test` -- All existing tests pass
4. Check build output logs for tool registration count (should be ~137)
  </verify>
  <done>
registry.ts updated with all Phase 5 imports and registrations. Build compiles with zero errors. All existing tests pass. Server reports approximately 137 registered tools (108 prior + 29 Phase 5).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. `npm test` passes (all existing tests)
4. Tool count is approximately 137 (108 + 29)
5. Registry follows established phase section pattern with comments
6. ReferenceDataCache instantiated with default 5-minute TTL
</verification>

<success_criteria>
- All Phase 5 tools registered in registry
- Build passes with zero errors
- All existing tests pass
- Tool count increased from 108 to ~137
- Registry organized by phase with clear comments
</success_criteria>

<output>
After completion, create `.planning/phases/05-products-lists/05-03-SUMMARY.md`
</output>

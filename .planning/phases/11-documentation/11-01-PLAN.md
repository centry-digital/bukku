---
phase: 11-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "README leads with npm/npx installation, not clone-and-build"
    - "README shows Quick Start that gets a user running in under 2 minutes"
    - "README has separate Claude Desktop and Claude Code configuration sections with npx-based config as primary"
    - "README includes conversational usage examples without exposing tool names"
    - "README includes tool categories table with category counts and capabilities"
    - "README has Development section with clone, install, build, test, and project structure"
  artifacts:
    - path: "README.md"
      provides: "Complete user-facing documentation for npm-distributed package"
      contains: "npx @centry-digital/bukku-mcp"
  key_links:
    - from: "README.md Quick Start"
      to: "npm package @centry-digital/bukku-mcp"
      via: "npx command"
      pattern: "npx @centry-digital/bukku-mcp"
    - from: "README.md Configuration"
      to: "Environment variables"
      via: "env var table"
      pattern: "BUKKU_API_TOKEN.*BUKKU_COMPANY_SUBDOMAIN"
---

<objective>
Restructure README.md around npm distribution as the primary installation path, with clear Quick Start, configuration walkthroughs for Claude Desktop and Claude Code, conversational usage examples, and a Development section for contributors.

Purpose: Users discovering the package on npm or GitHub can install, configure, and start using the server within 2 minutes.
Output: Complete README.md with all DOCS-01, DOCS-02, DOCS-03 requirements satisfied.
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentation/11-CONTEXT.md
@README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README around npm distribution</name>
  <files>README.md</files>
  <action>
Rewrite README.md with the following section order (per locked user decision):

1. **Title + one-line description** — "Bukku MCP Server" with brief explanation of what it does (MCP server connecting AI assistants to Bukku accounting). Include npm badge linking to package page.

2. **What can it do?** — Keep the existing conversational prompt examples (they're good). Keep the beginner-friendly, concise tone. Do NOT expose tool names or API patterns. Refine slightly if needed. Keep the 173-tool count and the existing categories table (same format — clean, scannable).

3. **Quick Start** — Zero-to-running in under 2 minutes:
   - Prerequisites: Node.js v18+, Bukku account with API access, MCP-compatible AI client
   - Step 1: Get your Bukku API token (same instructions as current README — log in, Control Panel > Integrations > API Access, note subdomain)
   - Step 2: Add to your AI client — show the npx-based Claude Desktop config as the primary example:
     ```json
     {
       "mcpServers": {
         "bukku": {
           "command": "npx",
           "args": ["-y", "@centry-digital/bukku-mcp"],
           "env": {
             "BUKKU_API_TOKEN": "your-token-here",
             "BUKKU_COMPANY_SUBDOMAIN": "your-subdomain"
           }
         }
       }
     }
     ```
   - Step 3: Restart your AI client — done!

4. **Installation** — Two paths:
   - **npx (recommended)** — Zero install, always latest version. Just configure and go (reference Quick Start config). Explain that npx downloads and runs the package on demand.
   - **npm global install** — For users who prefer a persistent installation:
     ```bash
     npm install -g @centry-digital/bukku-mcp
     ```
     Then config uses `"command": "bukku-mcp"` with no args (instead of npx).

5. **Configuration** — Structured as:
   - **Environment Variables** — Keep the existing env var table (BUKKU_API_TOKEN, BUKKU_COMPANY_SUBDOMAIN) with descriptions.
   - **Claude Desktop** — Config file paths for macOS and Windows. Show npx config as primary example, then npm global install config as "If you installed globally" alternative. Note: restart Claude Desktop after config change.
   - **Claude Code** — Config file path (`.claude/settings.json` or project-level). Show npx-based config. Show npm global alternative.
   - **Other MCP Clients** — Brief note that any MCP stdio client works. Show the npx command and env vars needed.

6. **Troubleshooting** — Update for npm-based setup:
   - "Configuration Error" on startup — check env vars
   - "Token validation failed" — regenerate token
   - "Server doesn't appear in client" — verify config JSON syntax, restart client
   - npx-specific: "Could not resolve package" — check Node.js version >= 18, check network/proxy
   - Permission errors with global install — suggest npx instead, or fix npm permissions

7. **Development** — For contributors (moved from top-level setup):
   - Clone and build:
     ```bash
     git clone https://github.com/centry-digital/bukku-mcp.git
     cd bukku-mcp
     npm install
     npm run build
     ```
   - Commands: `npm run build` (compile TypeScript), `npm test` (run tests), `npm start` (start server)
   - Project structure: Keep the existing directory tree (src/ with client, config, errors, tools, index.ts)

8. **License** — MIT (keep as-is)

IMPORTANT style notes:
- Conversational, beginner-friendly tone throughout
- No screenshots (text-only, lower maintenance, cross-platform)
- npx is ALWAYS shown first/primary, npm global install is the alternative
- Do NOT expose internal tool names (list-sales-invoices, create-contact, etc.) — users interact through natural language conversation
- Keep the tool categories table format exactly as-is (proven, scannable)
  </action>
  <verify>
Read the final README.md and verify:
1. First code block shows npx command (not git clone)
2. Sections appear in order: title, what can it do, quick start, installation, configuration, troubleshooting, development, license
3. Both Claude Desktop and Claude Code config sections exist with npx-based configs
4. Env var table is present with both variables
5. Tool categories table is present with all 9 categories
6. Development section has clone/install/build/test commands and project structure
7. No internal tool names appear (no "list-sales-invoices", "create-contact", etc.)
8. "npx @centry-digital/bukku-mcp" appears prominently
  </verify>
  <done>README.md is restructured with npm/npx as primary install path, Quick Start section, separate Claude Desktop/Code config sections, conversational usage examples, tool categories table, and Development section at the bottom</done>
</task>

<task type="auto">
  <name>Task 2: Validate README content accuracy</name>
  <files>README.md</files>
  <action>
Cross-reference the README content against package.json and project files to ensure accuracy:

1. Verify package name matches: `@centry-digital/bukku-mcp` (from package.json name field)
2. Verify bin name matches: `bukku-mcp` (from package.json bin field) — used in npm global install config
3. Verify Node.js version requirement matches: `>=18.0.0` (from package.json engines field)
4. Verify tool count: count actual registered tools by checking the tool registry or existing tool count references. The current README says 173; verify this is still accurate.
5. Verify repository URL matches: `https://github.com/centry-digital/bukku-mcp` (from package.json homepage)
6. Verify env var names match what the code actually reads: BUKKU_API_TOKEN and BUKKU_COMPANY_SUBDOMAIN (check src/config/)
7. Ensure the npx config JSON is valid JSON (no trailing commas, correct structure)
8. Ensure the npm global install config JSON uses "bukku-mcp" as the command (matching bin field)

Fix any discrepancies found.
  </action>
  <verify>
Run: `node -e "JSON.parse(require('fs').readFileSync('README.md','utf8').match(/```json\n([\s\S]*?)```/)?.[1] || '{}')"` to validate at least the first JSON block is parseable.
Grep README.md for "@centry-digital/bukku-mcp" — should appear multiple times.
Grep README.md for "bukku-mcp" (the bin command) — should appear in npm global config section.
  </verify>
  <done>All package names, versions, URLs, env var names, and JSON configs in the README are verified accurate against the actual package.json and source code</done>
</task>

</tasks>

<verification>
1. README.md exists and has been modified from the original clone-and-build version
2. `grep -c "npx" README.md` returns 3+ (npx appears in Quick Start, Installation, and Configuration sections)
3. `grep -c "git clone" README.md` returns exactly 1 (only in Development section)
4. README sections follow the locked ordering: What is it → Quick Start → Installation → Configuration → Troubleshooting → Development
5. No internal tool names leak into user-facing sections (grep for "list-" or "create-" or "update-" should only appear in Development context if at all)
</verification>

<success_criteria>
- DOCS-01 satisfied: README explains both npm global install and npx usage with example commands
- DOCS-02 satisfied: README documents environment variable setup and Claude Desktop/Code configuration
- DOCS-03 satisfied: README includes tool categories overview and conversational usage examples
- README is restructured around npm distribution as primary path
- Development/contributor workflow moved to bottom section
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation/11-01-SUMMARY.md`
</output>

---
phase: 04-banking-contacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/configs/bank-money-in.ts
  - src/tools/configs/bank-money-out.ts
  - src/tools/configs/bank-transfer.ts
  - src/tools/configs/contact.ts
  - src/tools/configs/contact-group.ts
  - src/tools/custom/contact-archive.ts

autonomous: true

must_haves:
  truths:
    - "Banking entity configs define correct API paths, response wrapper keys, filters, and business rules"
    - "Contact entity configs define correct API paths, custom response wrapper keys, and delete constraints"
    - "Contact archive/unarchive tools send is_archived boolean via PATCH, not status string"
  artifacts:
    - path: "src/tools/configs/bank-money-in.ts"
      provides: "Bank money in CrudEntityConfig"
      exports: ["bankMoneyInConfig"]
      contains: "apiBasePath: \"/banking/incomes\""
    - path: "src/tools/configs/bank-money-out.ts"
      provides: "Bank money out CrudEntityConfig"
      exports: ["bankMoneyOutConfig"]
      contains: "apiBasePath: \"/banking/expenses\""
    - path: "src/tools/configs/bank-transfer.ts"
      provides: "Bank transfer CrudEntityConfig with reduced filters"
      exports: ["bankTransferConfig"]
      contains: "listFilters: [\"account_id\"]"
    - path: "src/tools/configs/contact.ts"
      provides: "Contact CrudEntityConfig with custom wrapper keys"
      exports: ["contactConfig"]
      contains: "singularKey: \"contact\""
    - path: "src/tools/configs/contact-group.ts"
      provides: "Contact group CrudEntityConfig (minimal)"
      exports: ["contactGroupConfig"]
      contains: "singularKey: \"group\""
    - path: "src/tools/custom/contact-archive.ts"
      provides: "Custom archive/unarchive tools for contacts"
      exports: ["registerContactArchiveTools"]
      contains: "is_archived"
  key_links:
    - from: "src/tools/configs/bank-money-in.ts"
      to: "src/types/bukku.ts"
      via: "import CrudEntityConfig"
      pattern: "import type.*CrudEntityConfig"
    - from: "src/tools/custom/contact-archive.ts"
      to: "src/errors/transform.ts"
      via: "import error transformers"
      pattern: "import.*transformHttpError.*transformNetworkError"
---

<objective>
Create all Phase 4 entity configurations and custom tools.

Purpose: Define the 5 CrudEntityConfig objects (3 banking, 2 contacts) and 2 custom contact archive tools that Phase 4 requires. These are the building blocks wired into the registry in Plan 02.

Output: 6 new source files ready for registry integration.
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern references — follow these exactly
@src/tools/configs/purchase-bill.ts
@src/tools/configs/purchase-order.ts
@src/types/bukku.ts
@src/tools/factory.ts
@src/errors/transform.ts
@src/utils/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 banking entity configs</name>
  <files>
    src/tools/configs/bank-money-in.ts
    src/tools/configs/bank-money-out.ts
    src/tools/configs/bank-transfer.ts
  </files>
  <action>
Create 3 CrudEntityConfig files following the exact pattern in `src/tools/configs/purchase-bill.ts`.

**bank-money-in.ts:**
- entity: "bank-money-in"
- apiBasePath: "/banking/incomes"
- singularKey: "transaction", pluralKey: "transactions"
- description: "bank money in transaction"
- operations: all 5 CRUD operations
- hasStatusUpdate: true
- listFilters: ["contact_id", "account_id", "email_status"]
- businessRules.delete: "Only draft and void money in transactions can be deleted. Ready or pending approval transactions cannot be deleted — use update-bank-money-in-status to void a ready transaction instead."
- businessRules.statusTransitions: "Valid transitions: draft -> pending_approval, draft -> ready, pending_approval -> ready, ready -> void. A void transaction is final and cannot be changed. There is no way to revert from ready, pending_approval, or void back to draft."
- JSDoc: Document API path, response keys, and that Money In represents incoming cash transactions

**bank-money-out.ts:**
- entity: "bank-money-out"
- apiBasePath: "/banking/expenses"
- singularKey: "transaction", pluralKey: "transactions"
- description: "bank money out transaction"
- operations: all 5 CRUD operations
- hasStatusUpdate: true
- listFilters: ["contact_id", "account_id", "email_status"] (same as money in)
- businessRules: Same template as money in, replacing entity references with "money out"
- JSDoc: Document that Money Out represents outgoing cash transactions

**bank-transfer.ts:**
- entity: "bank-transfer"
- apiBasePath: "/banking/transfers"
- singularKey: "transaction", pluralKey: "transactions"
- description: "bank transfer"
- operations: all 5 CRUD operations
- hasStatusUpdate: true
- listFilters: ["account_id"] — ONLY account_id (no contact_id, no email_status per user decision)
- businessRules: Same template, replacing entity references with "transfer"
- JSDoc: Note that transfers are account-to-account (no contact), and have fewer list filters than money in/out

All banking entities use identical status lifecycle (same as sales/purchases — confirmed by shared description files in OpenAPI spec).
  </action>
  <verify>
Run `npx tsc --noEmit` — all 3 new files compile without errors. Each file exports a named CrudEntityConfig with the correct entity name.
  </verify>
  <done>
3 banking config files exist at src/tools/configs/bank-money-in.ts, bank-money-out.ts, bank-transfer.ts. Each has correct apiBasePath, response keys, filters, and business rules matching OpenAPI bank.yaml spec. Transfer config has only ["account_id"] in listFilters.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 2 contact entity configs</name>
  <files>
    src/tools/configs/contact.ts
    src/tools/configs/contact-group.ts
  </files>
  <action>
Create 2 CrudEntityConfig files following the established pattern but with custom response wrapper keys.

**contact.ts:**
- entity: "contact"
- apiBasePath: "/contacts"
- singularKey: "contact" (NOT "transaction" — first non-transaction entity)
- pluralKey: "contacts"
- description: "contact"
- operations: all 5 CRUD operations
- hasStatusUpdate: false — archive/unarchive handled by custom tools in contact-archive.ts, NOT factory status tool (factory sends `{ status: X }` but API expects `{ is_archived: boolean }`)
- listFilters: ["group_id", "status", "type", "is_myinvois_ready"] — per user decision (status has ALL/ACTIVE/INACTIVE values, type has customer/supplier/employee)
- businessRules.delete: "Only contacts with no linked transactions can be deleted. Archive instead if the contact has transaction history."
- No businessRules.statusTransitions (hasStatusUpdate is false, custom tools handle archive)
- JSDoc: Document that contacts use contact/contacts response wrappers (not transaction), and that archive is handled by separate tools

**contact-group.ts:**
- entity: "contact-group"
- apiBasePath: "/contacts/groups"
- singularKey: "group" (NOT "transaction")
- pluralKey: "groups"
- description: "contact group"
- operations: all 5 CRUD operations
- hasStatusUpdate: false (groups have no status operations per OpenAPI spec)
- listFilters: [] (no entity-specific filters; factory provides base pagination)
- No businessRules needed (no delete constraints, no status operations)
- JSDoc: Document that groups organize contacts into categories, simplest entity config
  </action>
  <verify>
Run `npx tsc --noEmit` — both files compile. Confirm contact config has `hasStatusUpdate: false` and `singularKey: "contact"`. Confirm contact-group config has `listFilters: []` and `singularKey: "group"`.
  </verify>
  <done>
2 contact config files exist at src/tools/configs/contact.ts and contact-group.ts. Contact uses custom wrapper keys (contact/contacts), hasStatusUpdate false, and "no linked transactions" delete constraint. Contact group is minimal with no filters and no status.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create custom contact archive/unarchive tools</name>
  <files>
    src/tools/custom/contact-archive.ts
  </files>
  <action>
Create the `src/tools/custom/` directory and the `contact-archive.ts` file.

This file contains a `registerContactArchiveTools(server, client)` function that registers 2 MCP tools:

**archive-contact:**
- Name: "archive-contact"
- Description: "Archive a contact (hide from active lists). Use this for contacts with transaction history instead of deleting them."
- Input schema: `{ id: z.number().describe("The contact ID") }`
- Handler: `client.patch(\`/contacts/${params.id}\`, { is_archived: true })`
- Return: JSON stringified result
- Error handling: Same pattern as factory — check `error instanceof Response` for HTTP errors, else network error

**unarchive-contact:**
- Name: "unarchive-contact"
- Description: "Unarchive a contact (restore to active lists)."
- Input schema: `{ id: z.number().describe("The contact ID") }`
- Handler: `client.patch(\`/contacts/${params.id}\`, { is_archived: false })`
- Same return and error handling pattern

**Function signature:** `registerContactArchiveTools(server: McpServer, client: BukkuClient): number`
- Returns 2 (number of tools registered)
- Logs each registered tool via `log("Registered tool: archive-contact")` and `log("Registered tool: unarchive-contact")`

**Imports needed:**
- `McpServer` from "@modelcontextprotocol/sdk/server/mcp.js"
- `z` from "zod"
- `BukkuClient` from "../../client/bukku-client.js"
- `transformHttpError`, `transformNetworkError` from "../../errors/transform.js"
- `log` from "../../utils/logger.js"

Follow the exact error handling pattern from factory.ts lines 78-84 (the `catch (error)` block structure).
  </action>
  <verify>
Run `npx tsc --noEmit` — file compiles. Confirm function returns number type. Confirm both tools send `is_archived` boolean (not `status` string) in PATCH body.
  </verify>
  <done>
src/tools/custom/contact-archive.ts exists with registerContactArchiveTools function exporting 2 tools (archive-contact, unarchive-contact). Both use PATCH with is_archived field. Error handling matches factory pattern.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — all 6 new files compile with zero errors
2. Each banking config has entity name matching locked naming convention (bank-money-in, bank-money-out, bank-transfer)
3. Each banking config uses apiBasePath matching locked API paths (/banking/incomes, /banking/expenses, /banking/transfers)
4. Bank transfer config has listFilters: ["account_id"] only (no contact_id, no email_status)
5. Contact config has singularKey: "contact" (not "transaction") and hasStatusUpdate: false
6. Contact group config has singularKey: "group" and listFilters: []
7. Custom archive tools send `{ is_archived: true/false }` not `{ status: "..." }`
</verification>

<success_criteria>
All 6 source files compile, follow established patterns, and are ready for registry wiring in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/04-banking-contacts/04-01-SUMMARY.md`
</output>

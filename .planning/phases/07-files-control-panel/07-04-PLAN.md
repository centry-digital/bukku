---
phase: 07-files-control-panel
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/custom/control-panel-archive.ts
  - src/tools/registry.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Only archive-location and unarchive-location tools exist (no tag/tag-group archive tools)"
    - "registerControlPanelArchiveTools returns 2"
    - "Registry JSDoc reflects 20 Phase 7 tools and 169 total tools"
    - "Project builds successfully with npm run build"
  artifacts:
    - path: "src/tools/custom/control-panel-archive.ts"
      provides: "Location-only archive/unarchive tools"
      contains: "return 2"
    - path: "src/tools/registry.ts"
      provides: "Correct tool counts in JSDoc and comments"
      contains: "169"
  key_links:
    - from: "src/tools/registry.ts"
      to: "src/tools/custom/control-panel-archive.ts"
      via: "registerControlPanelArchiveTools import and call"
      pattern: "registerControlPanelArchiveTools"
---

<objective>
Remove 4 non-functional archive tools (archive-tag, unarchive-tag, archive-tag-group, unarchive-tag-group) that call PATCH on API endpoints that do not exist in the Bukku API.

Purpose: UAT tests 10 and 12 revealed that the Bukku API only defines PATCH for /location/{id}, not for /tags/{id} or /tags/groups/{id}. The 4 tag/tag-group archive tools will always fail. Removing them prevents misleading tool availability and corrects tool counts.

Output: Clean build with 169 total tools (was 173), Phase 7 contributing 20 tools (was 24).
</objective>

<execution_context>
@/Users/ylchow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ylchow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-files-control-panel/07-UAT.md
@src/tools/custom/control-panel-archive.ts
@src/tools/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove non-functional tag/tag-group archive tools and update archive module</name>
  <files>src/tools/custom/control-panel-archive.ts</files>
  <action>
Edit src/tools/custom/control-panel-archive.ts:

1. Update the file-level JSDoc comment block (lines 7-13): Change from "Locations, tags, and tag groups" to "Locations only". Explain that the Bukku API only defines PATCH for /location/{id} -- tags and tag groups do not have PATCH endpoints.

2. Update the function-level JSDoc (lines 15-21): Change the @returns comment from "Number of tools registered (always 6)" to "Number of tools registered (always 2)".

3. Remove the archive-tag tool definition (lines 88-117, the server.tool call for "archive-tag" and its log line).

4. Remove the unarchive-tag tool definition (lines 120-148, the server.tool call for "unarchive-tag" and its log line).

5. Remove the archive-tag-group tool definition (lines 150-179, the server.tool call for "archive-tag-group" and its log line).

6. Remove the unarchive-tag-group tool definition (lines 181-210, the server.tool call for "unarchive-tag-group" and its log line).

7. Change the return statement from `return 6` to `return 2`.

Keep archive-location (lines 27-55) and unarchive-location (lines 58-86) completely unchanged.

The resulting file should have: imports, updated JSDoc, function signature, archive-location tool, unarchive-location tool, return 2.
  </action>
  <verify>
Run: `npx tsc --noEmit` -- should compile without errors.
Verify the file contains exactly 2 server.tool() calls (for archive-location and unarchive-location only).
Verify the file ends with `return 2;`.
  </verify>
  <done>
control-panel-archive.ts contains only archive-location and unarchive-location tools. No references to /tags/ or /tags/groups/ endpoints remain. Return value is 2. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update registry JSDoc tool counts and comments</name>
  <files>src/tools/registry.ts</files>
  <action>
Edit src/tools/registry.ts to correct tool counts after removing 4 tools:

1. In the Tool Registry JSDoc block (around line 77), update the Phase 7 line from:
   "Phase 7 added file + control panel entity configs (4 entities, 14 factory tools + 1 custom upload tool + 3 custom location tools + 6 custom archive tools = 24 tools)."
   to:
   "Phase 7 added file + control panel entity configs (4 entities, 14 factory tools + 1 custom upload tool + 3 custom location tools + 2 custom archive tools = 20 tools)."

2. Update the inline comment on the registerControlPanelArchiveTools call (around line 172) from:
   "// Custom control panel archive tools (Phase 7) — 6 tools (archive/unarchive for locations, tags, tag groups)"
   to:
   "// Custom control panel archive tools (Phase 7) — 2 tools (archive/unarchive for locations only)"

No other changes needed. The totalTools counter is dynamic (uses return values), so the actual count will automatically adjust from 173 to 169.
  </action>
  <verify>
Run: `npm run build` -- should compile and build successfully.
Run: `node -e "import('./dist/tools/registry.js').then(m => console.log('registry loaded'))"` -- should load without errors.
Grep registry.ts to confirm no remaining references to "6 custom archive" or "24 tools" in Phase 7 context.
  </verify>
  <done>
Registry JSDoc correctly documents Phase 7 as 20 tools (not 24). Inline comment for archive tools says 2 tools (not 6). Build succeeds. Total tool count is 169 at runtime.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -c "server.tool" src/tools/custom/control-panel-archive.ts` returns 2
3. `grep "return 2" src/tools/custom/control-panel-archive.ts` confirms return value
4. `grep -v "tags" src/tools/custom/control-panel-archive.ts | wc -l` confirms no tag endpoint references remain (or grep for "/tags/" returns 0 matches)
5. `grep "20 tools" src/tools/registry.ts` confirms updated Phase 7 count in JSDoc
6. `grep "2 tools" src/tools/registry.ts | grep -i archive | grep -i "control panel"` confirms inline comment updated
</verification>

<success_criteria>
- Build passes with 0 errors
- control-panel-archive.ts registers exactly 2 tools (archive-location, unarchive-location)
- No PATCH calls to /tags/{id} or /tags/groups/{id} exist in the codebase
- Registry JSDoc accurately reflects 20 Phase 7 tools
- UAT gaps from tests 10 and 12 are closed (non-functional tools removed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-files-control-panel/07-04-SUMMARY.md`
</output>
